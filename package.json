{
  "name": "pulse-v2",
  "version": "0.1.0",
  "private": true,
  "description": "Pulse v2 — AI-powered pre-sale platform for Schell Brothers",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "npm run dev --workspace=apps/web",
    "build": "npm run build --workspace=apps/web",
    "lint": "npm run lint --workspace=apps/web"
  },
  "engines": {
    "node": ">=20"
  }
}
